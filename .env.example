# Template only: no usable credentials belong in this file.
# Real .env and .env.* files are ignored and must never be committed.
# Copy to .env only when local configuration is needed.
# Configuration names will be confirmed as services are implemented.

# PostgreSQL connection string; intentionally empty (may contain credentials).
DATABASE_URL=
